import { Link } from "react-router-dom";
import { GetOut } from "../services/GetOut";

function AppLogOut() {
  return (
    <div className='container'>
        {/* <button style={{color: "blue", fontSize: 40}} onClick={GetOut()}>
            Need Log Out ?
        </button> */}
    </div>
  );
}

export default AppLogOut;